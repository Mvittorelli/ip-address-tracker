


function idk(hour, sec, min) {
    return (`Hours in one sec will be ${hour} : ${min} : ${sec}`)
};

console.log(idk(prompt("hour pls"),
    prompt("minute pls"),
    prompt("sec pls")));

    function idk(hour, min, sec) {
        return (`Hours in one sec will be ${hour} : ${min} : ${sec}`);
    }
    
    console.log(idk(
        prompt("hour pls"),
        prompt("minute pls"),
        prompt("sec pls")
    ));